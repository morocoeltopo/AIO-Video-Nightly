<?xml version="1.0" encoding="utf-8"?>
<!-- Merge layout for ExoPlayer video player surface and overlay components -->
<merge xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto">

	<!-- Container for maintaining video aspect ratio -->
	<androidx.media3.ui.AspectRatioFrameLayout
		android:id="@id/exo_content_frame"
		android:layout_width="match_parent"
		android:layout_height="match_parent"
		android:layout_gravity="center">

		<!-- Shutter view (background when video not playing) -->
		<View
			android:id="@id/exo_shutter"
			android:layout_width="match_parent"
			android:layout_height="match_parent"
			android:background="@color/color_primary" />

		<!-- Album art/thumbnail image (for audio tracks) -->
		<ImageView
			android:id="@id/exo_artwork"
			android:layout_width="match_parent"
			android:layout_height="match_parent"
			android:importantForAccessibility="no"
			android:scaleType="fitXY" />

		<!-- Subtitle/caption display area -->
		<androidx.media3.ui.SubtitleView
			android:id="@+id/exo_subtitles"
			android:layout_width="match_parent"
			android:layout_height="match_parent" />

		<!-- Buffering progress indicator -->
		<ProgressBar
			android:id="@id/exo_buffering"
			android:layout_width="wrap_content"
			android:layout_height="wrap_content"
			android:layout_gravity="center"
			android:indeterminate="true"
			app:buffered_color="@color/color_yellow_accent" />

		<!-- Error message display -->
		<TextView
			android:id="@id/exo_error_message"
			android:layout_width="match_parent"
			android:layout_height="match_parent"
			android:layout_gravity="center"
			android:background="@color/transparent_dark"
			android:gravity="center"
			android:padding="16dp"
			android:textColor="@color/transparent_white" />

	</androidx.media3.ui.AspectRatioFrameLayout>
</merge>